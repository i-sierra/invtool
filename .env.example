# App
INV_APP_NAME="Inventory Manager"
INV_ENV="dev"
INV_DEBUG=true

# DB (sync SQLAlchemy for simplicity; FastAPI runs sync work in threadpool)
INV_DATABASE_URL="sqlite:///./instance/app.db"

# Upload limits
INV_MAX_UPLOAD_MB=10
